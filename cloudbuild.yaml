steps:
  # Install
  - name: "gcr.io/cloud-builders/npm"
    args: ["install"]

  # Build
  - name: "gcr.io/cloud-builders/npm"
    args: ["run", "build"]

  # Deploy
  - name: "gcr.io/firebase-hosting-286313/firebase"
    args: ["deploy", "--only=hosting"]
